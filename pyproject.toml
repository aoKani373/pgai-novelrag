[project]
name = "pgai-novelrag"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.0",
    "pgai[sqlalchemy]>=0.12.1",
    "sqlmodel>=0.0.31",
]
